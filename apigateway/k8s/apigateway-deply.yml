#버전
apiVersion: apps/v1
kind: Deployment
metadata:
  name: apigateway-deployment
  namespace: lgcns
    

spec: 
  # 자원 공간의 낭비를 방지 및 관리의 편의성을 제공해주는 옵션
  revisionHistoryLimit: 2

  replicas: 1
  selector:
    matchLabels:
      app: apigateway

  template:
    metadata:
      labels:
        app: apigateway

    spec:
    #docker container
      containers:
      - name: apigateway-container
        # ERC 경로
        image: 933090817123.dkr.ecr.us-east-2.amazonaws.com/api-gateway:latest
        ports:
          - containerPort: 8080
  

---  #구분 지어야 함

#core service setting
apiVersion: v1

kind: Service

metadata:
  name: apigateway-service
  namespace: lgcns

spec: 
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080        # container port를 의미*
  selector:
    app: apigateway    #selector의 name